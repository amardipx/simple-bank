version: "1"

packages:
  - name: "db"
    path: "internal/db"
    queries: "db/queries"
    schema: "db/migrations"
    engine: "postgresql"
    emit_json_tags: true
    emit_interface: false
    emit_prepared_queries: false
    emit_exact_table_names: false
